# 作業セッション記録 — 2026-04-04

---

## 本日の成果サマリー

### 1. Vercel本番デプロイ完了 ✅

**できるようになったこと:**
- CAIROが `https://cairo-zybd.vercel.app/` で本番稼働中
- GoogleアカウントでのログインがVercel本番環境で動作
- Supabaseとの接続（DB保存・認証）が本番環境で稼働

**やったこと:**
- Vercelに必要な環境変数9つを設定
  - `ANTHROPIC_API_KEY` / `NEXTAUTH_SECRET` / `NEXTAUTH_URL`
  - `NEXT_PUBLIC_SUPABASE_URL` / `NEXT_PUBLIC_SUPABASE_ANON_KEY` / `SUPABASE_SERVICE_ROLE_KEY`
  - `GOOGLE_CLIENT_ID` / `GOOGLE_CLIENT_SECRET` / `INTERNAL_API_SECRET`
- Google Cloud ConsoleにVercel本番URLのリダイレクトURIを追加
- ドキュメント内の本番URLを `cairo.vercel.app` → `cairo-zybd.vercel.app` に全更新

---

### 2. REQSテナントの基盤構築 ✅

**できるようになったこと:**
- SupabaseにREQSテナントが存在する
- REQSテナント用にAI社員6体が登録済み
- 管理画面（`/admin`）からREQSのトンマナをAI社員ごとに設定できる
- トンマナを設定すると、以降の全出力にREQSの文体・スタイルが自動反映される

**やったこと:**
- Supabaseに `tenants` レコードを作成（`id: bfd74c8b-0897-4eb0-b588-6107722fa1f3`）
- `tenant_agents` にREQS×6エージェントを登録
- `app/api/execute/route.ts` を修正 — `system_prompt_suffix` をDBから取得してシステムプロンプトに自動注入するロジックを追加

---

### 3. プロンプト設計の整理・手順書化 ✅

**できるようになったこと:**
- プロンプトがどこに何を書けばいいか構造が明確になった
- REQSの業務メニュー15種類の設計が完了（実装待ち）

**やったこと:**
- `docs/REQS_TENANT_IMPLEMENTATION.md` を作成
  - プロンプト3層構造の解説
  - REQSトンマナの書き方・設定方法
  - 15メニューの一覧と設計
  - Step 1〜6の実装手順書

---

## プロンプト3層構造（重要）

```
システムプロンプト =
  ① キャラクター設定（data/characters.ts）固定
  + ② REQSトンマナ（Supabase: tenant_agents.system_prompt_suffix）← 今日配線した

ユーザープロンプト =
  ③ 業務指示（data/menus.ts の promptTemplate）
  + ④ ユーザーの入力値
```

---

## 残タスク

| タスク | 状態 | 担当 |
|---|---|---|
| REQSの15メニューをdata/menus.tsに追加 | 未着手 | 次回 |
| 管理画面でREQSのトンマナ文章を設定 | 未着手 | 次回 |
| テナントごとのユーザー管理（招待・ログイン） | 別エージェントが対応中 | - |
| テナントごとのメニュー分離（Supabase menus テーブル） | 設計済み・未実装 | Phase 3 |

---

## 本番URL・接続情報

| 項目 | 値 |
|---|---|
| 本番URL | https://cairo-zybd.vercel.app/ |
| GitHub | https://github.com/yutamaruyama137-lgtm/cairo |
| REQSテナントID | `bfd74c8b-0897-4eb0-b588-6107722fa1f3` |
| Supabaseプロジェクト | `mkmxzhntozwiaeexphhq` |

---

*記録: JARVIS — 2026-04-04*
